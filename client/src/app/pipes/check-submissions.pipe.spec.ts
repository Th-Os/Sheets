import { CheckSubmissionsPipe } from './check-submissions.pipe';

describe('CheckSubmissionsPipe', () => {
  it('create an instance', () => {
    const pipe = new CheckSubmissionsPipe();
    expect(pipe).toBeTruthy();
  });
});
