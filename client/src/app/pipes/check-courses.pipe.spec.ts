import { CheckCoursesPipe } from './check-courses.pipe';

describe('CheckCoursesPipe', () => {
  it('create an instance', () => {
    const pipe = new CheckCoursesPipe();
    expect(pipe).toBeTruthy();
  });
});
