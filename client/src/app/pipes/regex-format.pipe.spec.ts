import { RegexFormatPipe } from './regex-format.pipe';

describe('RegexFormatPipe', () => {
  it('create an instance', () => {
    const pipe = new RegexFormatPipe();
    expect(pipe).toBeTruthy();
  });
});
