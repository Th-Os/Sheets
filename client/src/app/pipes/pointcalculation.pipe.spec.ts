import { PointcalculationPipe } from './pointcalculation.pipe';

describe('PointcalculationPipe', () => {
  it('create an instance', () => {
    const pipe = new PointcalculationPipe();
    expect(pipe).toBeTruthy();
  });
});
